services:

  web:
    build: .
    container_name: flask_app
    restart: unless-stopped
    env_file: .env
    environment:
      MYSQL_HOST: flask-db.c76os2wem2n6.ap-south-1.rds.amazonaws.com
      MYSQL_PORT: 3306
      MYSQL_USER: admin
      MYSQL_PASSWORD: flask-db-web
      MYSQL_DATABASE: flaskdb
    ports:
      - "${FLASK_PORT:-5000}:5000"
